{
  "dependencies": {
    "concurrently": "^7.1.0"
  },
  "scripts": {
    "start": "npm i && concurrently \"npm i --prefix ./backend && npm run devStart --prefix ./backend\" \"npm i --prefix ./frontend && npm start --prefix ./frontend\""
  }
}
