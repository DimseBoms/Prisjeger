{
  "dependencies": {
    "axios": "^0.26.1",
    "concurrently": "^7.1.0"
  },
  "scripts": {
    "start": "npm i && concurrently \"npm i --prefix ./backend && npm start --prefix ./backend\" \"npm i --prefix ./frontend && npm start --prefix ./frontend\""
  }
}
